<section>
    <%# user data %>
    <%= render 'shared/user', user: @user%>

    <ul">
       <% @posts.each do |post| %>
        <li class="border list-none mt-4 hover:bg-slate-200">
            <%= link_to user_post_path(@user, post) do %>
                <%= render 'shared/post', post: post%>
                <hr>
                <%= render 'shared/name_comment', comments: post.retrieve_recent_comments %>
            <% end %>
        </li>
       <% end %>
    </ul>

    <button class="bg-slate-500 text-white px-3 py-1 rounded shadow mt-5 hover:bg-slate-600 active:scale-105">Pagination</button>

</section>